#include <bits/stdc++.h>
using namespace std;

void solve() {
    int t, a, b;
    cin >> t;
    while (t-- > 0) {
        cin >> a >> b;
        cout << a + b << "\n";
    }
}

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0); cout.tie(0);
  solve();
}